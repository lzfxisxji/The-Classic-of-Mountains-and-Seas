# 2026-08-07 工作日志

## 修复：档案弹窗内容错位 bug
- 现象：点击不同异兽卡片后，Hero「展开异兽档案」按钮打开的对话框始终显示赤羽应龙。
- 根因：`js/app.js` 第 136 行卡片点击只绑定 `setSpecimen()`（仅切换 Hero 预览，不改 `current`），而 `#open-record` 按钮绑定 `openRecord(current)`，`current` 默认 `'yinglong'`，导致对话框退回默认应龙。
- 修复：将卡片点击改为 `openRecord(card.dataset.beast)`，使点击卡片直接打开该异兽研究档案（`openRecord` 内部仍调用 `setSpecimen` 更新 Hero + current，再渲染并弹窗）。
- 影响范围：`js/app.js` 单行改动，data.js / index.html / 全部 CSS 未动。
- 验证：`node --check` 通过；本地预览 http://localhost:8211

## 响应式补全：断点体系 + 触摸适配 + 大屏收口
- 背景：winner 希望网站自动适配访问者设备屏幕，问是否用 Bootstrap。结论：不引入框架，原生 CSS 媒体查询即可（浏览器按视口自动适配，无需 UA 嗅探）。
- 改动文件：`css/tokens.css`（顶部新增「响应式断点体系」注释文档：430/780/1024/1440 四档基准）、`css/responsive.css`（重写）。
- `responsive.css` 原仅有 ≤780 / ≤430 / 减动效 三档 + 一段重复的 780 specimen-art 块。
- 新增：`@media (max-width:1024px)` 平板档（Hero 改 min-height 自适应防矮视口裁切、异兽插画/展柜图放宽防裁）、`@media (hover:none) and (pointer:coarse)` 触摸适配（隐藏 hover tooltip 改由点击节点更新图例、`:active` 轻缩放反馈、去 `-webkit-tap-highlight`）、`@media (min-width:1440px)` 大屏收口（records/map-section padding 上限、record-rail max-width 1500 居中）。
- 合并原重复 780 块为单块。媒体查询改为 max-width 递减排列（小断点后写覆盖大断点）。
- 验证：6 模块括号全部配对；关键规则覆盖检查通过；`max-width:780px` 仅 1 处。
- 约束：未改 index.html、组件视觉风格、Hero 结构、地图结构。

## 页面优化审计（21:43）
- winner 反馈：当前网页设计"还是普通"，要求找出可优化/待设计的页面。
- 现状诊断：网站本质是"异兽罗列器"——仅 6 板块（顶栏/Hero/档案索引/观测地图/档案抽屉/页脚），缺《山海经》背景、叙事、搜索筛选；异兽仅 4 只且 3 只无原创插画。
- 现有 6 板块升级方向：顶栏(移动菜单是摆设需接7锚点)、Hero(永远应龙/单调→序厅化)、档案索引(无搜索无分类)、观测地图(最出彩保留+加信息密度)、档案抽屉(结构好但白泽等字段待补全=填内容)、页脚(信息少+声明待改AI图)。
- 建议新建 5 板块：山海经概述(P2序厅)、山经水经体系(P3地理图谱呼应铜盘)、神话叙事(P2横向故事流沉浸感)、文化影响(P3古今对照)、检索与筛选(P1全局搜索+分类+排序)。
- 优先级：P1 检索筛选+顶栏移动菜单 → P2 Hero序厅+概述+叙事 → P3 体系+影响。
- 产出：页面优化地图可视化（左现有诊断/右新建方向）；未改代码，等 winner 决定从哪入手。

## P1 落地：顶栏移动菜单 + 检索筛选（已完成）
- winner 选 P1 为首个优化方向。约束：不动 Hero/地图结构与视觉风格。
- 改动文件：`index.html`、`css/components.css`（汉堡变 X 动画）、`css/sections.css`（records-toolbar/search/filter-chip/empty 样式 + `.mobile-nav{display:none}` 基准）、`css/responsive.css`（`≤780px` 内加移动面板/工具栏堆叠 + `≥781px` 隐藏 `.menu`）、`js/app.js`（菜单切换 + 搜索/分类过滤）。
- `index.html`：header 后加 `<nav class="mobile-nav" id="mobile-nav">`（异兽档案/古卷索引/观测地图 三锚点）；records 区加 `.records-toolbar`（搜索框 `#records-search` + 5 个 `.filter-chip` 全部/南山经/东山经/北山经/东海经）；4 张 `.record-card` 加 `data-category`；加 `.records-empty` 空态提示。
- 交互：`menuButton` 切换 `.open`（汉堡→X）+ aria-expanded/aria-label；点击移动导航链接自动收起；搜索按「编号+名+卡号+经部」模糊匹配，分类 chip 单选，二者 AND 组合，无结果显空态。
- 验证：`node --check js/app.js` 通过；6 CSS 模块括号全部配对；关键 DOM id/class 与 JS 选择器一一对应。
- 约束遵守：Hero/地图未动；沿用墨底金线 DM Mono 设计语言。
- 待 winner 本地起服务自测（沙箱服务不可达其本机；ES Module 需 http 协议，不能直接 file:// 打开）。

## P2 落地：Hero 序厅化 + 山海经概述（已完成）
- winner 选 P2 为解决「设计还是普通」的首批优化（原优化方案板块2）。目标：补《山海经》背景与叙事，把单只异兽海报改造成博物馆序厅。
- 改动文件：`index.html`、`css/sections.css`（hero-wall/hero-intro/hero-scroll + 整套 .overview 样式 + [data-reveal] 揭示态）、`css/responsive.css`（概述板块 ≤1024/≤780/≤430 适配、隐藏 hero-wall、减动效兜底）、`js/app.js`（追加 IntersectionObserver 滚动揭示，无 IO 时直接显示）。
- Hero 序厅化：新增 `.hero-wall`（左侧竖排「山海档案局/序厅 PROLOGUE HALL」墙面标）、`.hero-intro`（顶部博物馆陈述行）、`.hero-scroll`（「展开卷轴」下滚指引，chevron 上下浮动动画）；主导航与移动导航均加「山海概述」#overview 锚点。应龙展示与 data-field 切换逻辑未动。
- 山海经概述板块（#overview，置于 Hero 与古卷索引之间）：导语 + 四项数据（18卷/3.1万字/战国–汉/百科）+ 十八卷体系可视化（五藏山经5 + 海经13，clip-path 切角框 + 悬停高亮）+ 鲁迅评语引言 + 价值说明。沿用墨底金线/DM Mono/Noto Serif SC。
- 关键处理：Hero 内 hero-intro/hero-scroll 不挂 data-reveal（hero-copy 容器自身有 reveal 动画，挂了会双重隐藏）；data-reveal 仅用于概述板块元素，靠 IO 揭示；减动效下 [data-reveal] 强制显示。
- 验证：node --check app.js 通过；6 CSS 模块括号全部配对（sections 221/221、responsive 102/102 等）。
- 待 winner 本地起服务自测（Live Server / python -m http.server 8211）。

## 补齐异兽卡片原创线描插画（已完成）
- winner 确认 P2 稳了，要求先把白泽/精卫/夔牛的原创插画补上（档案索引卡仍用「鹿/鸟/夔牛」文字占位）。
- 事实核查：assets/ 下 dragon.png / baize.png / jingwei.png / kuiniu.png 已是高质量 AI 档案原画（墨底金线观测卡风格），缺口只在 `.record-card` 右下角的 `.card-art` 水印还是文字。
- 改动文件：`index.html`、`css/components.css`。
- `index.html`：4 张 `.record-card` 的 `.card-art` 全部替换为内联 SVG 线描小插画（应龙翼龙、白泽独角神鹿、精卫衔枝飞鸟、夔牛独足雷牛），使用 yinglong-art / baize-art / jingwei-art / kuiniu-art 类控制颜色；SVG 加 `aria-hidden="true"` 保持装饰语义。
- `css/components.css`：`.card-art` 从 font-size 91px 文字水印改为 86×86 SVG 容器；去 `.deer-art/.bird-art/.ox-art`；新增四兽颜色（gold/green-gold/red-gold/blue-gold）与透明度；`.kuiniu-art` 略小（72×72）避免双字占位变空后比例失调；加 `pointer-events:none` 防止水印拦截点击。
- 验证：`node --check js/app.js` 通过；6 CSS 模块括号全部配对；line 97 的 4 个 SVG 标签 `<svg>/<g>/<path>/<circle>` 自闭合与关闭标签数量一致（4/4/4/4）。
- 待 winner 本地起服务自测（Live Server / python -m http.server 8211），重点看档案索引卡的右下角线描图标。
