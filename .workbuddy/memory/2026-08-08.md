# 山海档案局 — 工作日志 2026-08-08

## P3 山经水经体系 + 文化影响（完成）
- 在「概述」与「古卷索引」间新增 #canon 板块：原创五方+四海罗盘 SVG（aria-hidden 装饰）+ 经部卡片网格（五藏山经5 + 海经3）。已收录经部（南山/北山/东山/东海）卡片带 data-go-filter，点击联动 P1 的 filter-chip 跳到 #records 并筛选；未收录（西/中山经、海内海外、大荒海内）加 is-locked 虚线禁用态，避免空态误导。
- 在「观测地图」后新增 #legacy 文化影响板块：三维卡（文学源流/影视游戏/学术研究）+ 历代校注时间线（郭璞/毕沅/郝懿行/袁珂）。
- 主导航与移动导航加「山经体系」「文化影响」锚点（桌面 nav gap 在 ≤1024 收到 18px 防拥挤）。
- JS（app.js 末尾 IIFE initCanonFilter）：监听 [data-go-filter] 点击，激活对应 chip、清空搜索、applyRecordsFilter，配合 href=#records 平滑滚动。
- CSS：sections.css 新增 .canon/.legacy 全套（复用墨底金线切角 clip-path、overview 标题风）；responsive.css 在 ≤1024/≤780/≤430 加罗盘/卡片/三卡/时间线堆叠适配。
- 校验：node --check app.js OK；6 模块 CSS 括号配对（sections 271/271、responsive 113/113）；SVG 标签平衡；data-go-filter 值与 filter-chip data-filter 完全一致。
- 未改 Hero/地图结构与视觉；沿用设计语言。
- 待 winner 本地自测（Live Server / python -m http.server 8211）。

## GitHub 部署上线（完成）
- 问题：git push 报 `Repository not found` / 连接超时，因 remote URL 连字符被复制为非 ASCII 字符（`The‑Classic‑of‑Mountains‑and‑Seas`）。
- 修复：`git remote set-url origin https://github.com/lzfxisxji/The-Classic-of-Mountains-and-Seas.git`（普通 ASCII 连字符）。
- 推送：`git push -u origin master` 成功，本地 master 跟踪 origin/master。
- 启用 GitHub Pages：`gh api -X POST repos/.../pages -f source[branch]=master -f source[path]=/`，仓库为 public，部署源 master 根目录。
- 访问地址：https://lzfxisxji.github.io/The-Classic-of-Mountains-and-Seas/
- 校验：仓库根目录包含 index.html、js/、css/、assets/，结构正确；Pages 首次构建状态 building，通常 1-3 分钟后可访问。
- 后续更新：本地修改后执行 `git push` 即可自动重新部署。

## 补内容：文案润色 + 解锁山经卡片（完成）
- 重写白泽/精卫/夔牛 research/quote：更扎实、有辨识度、去模板套话（data.js）。应龙保持不变。
- 新增 2 异兽：bifang 毕方（西山经·西次三经·火兆一足鸟，accent #c8531f）、fuzhu 夫诸（中山经·中次三经·四角白鹿大水兆，accent #3f8a78），字段齐全含 stats。
- 原创 SVG 档案原画：assets/bifang.svg、assets/fuzhu.svg（墨底+鎏金切角框+线描异兽+标题印章，对齐现有 PNG 原画风，作抽屉大图 img 源）。
- HTML 接入：record-rail 加 2 张 .record-card（含 card-art 线描 SVG + data-category 西山经/中山经）；map 加 2 个 .map-region 节点 + 2 条 map-route；filter-chip 加 西山经/中山经；records 计数 04→06、map 标题 04→06 SEALED MARKERS。
- canon 解锁：西山经/中山经 从 is-locked 改可点击 a canon-card + data-go-filter="西山经/中山经"，计数改「已收录 1 异兽」。海内·海外、大荒·海内仍锁定。
- CSS：components.css 加 .bifang-art（火橙）/ .fuzhu-art（青绿）描边色。
- 校验：data.js ES module 语法 OK；6 CSS 模块括号配对（components 81/81 等）；2 SVG 标签平衡；record-card↔filter-chip↔canon go-filter 完全一致（6 卡 / 7 chip / 6 联动）。
- 未改 hero（仍应龙）。相关档案按钮随 beasts 自动包含新兽。

## P4 神话叙事板块（完成）
- 位置：观测地图(#map) 与 文化影响(#legacy) 之间新增 #myth 板块，形成「地理→故事→影响」叙事递进。
- 主导航 + 移动导航在「观测地图」后加「神话叙事」(#myth) 锚点（桌面 nav gap ≤1024 已收到 18px，容纳无碍）。
- 三卷轴结构（复用 data-reveal 滚动揭示 + 切角 clip-path + 墨底金线）：卷头（MYTH·NO + 故事名 + 经部出处）/ 卷体（古籍原文左 朱砂边 / 现代释义右）/ 卷脚（意象 tag + 关联按钮）。
- 三故事：精卫填海（--jingwei 朱砂，data-open-beast="jingwei" 关联已存在的 jingwei 档案抽屉）/ 夸父逐日（--kuafu 橙金，待收录）/ 刑天舞干戚（--xingtian 铁青，待收录）。夸父、刑天无档案数据，仅做「异兽待收录」虚线禁用标注。
- JS（app.js 末尾 IIFE initMythLinks）：监听 [data-open-beast] 点击 → openRecord(beast) 复用现有抽屉逻辑（无写新兽数据）。
- CSS：sections.css 新增 .myth / .myth-scroll（--myth-accent/--myth-accent-strong 三色变量）/ 卷头卷体卷脚 / .myth-tag / .myth-link（含 --locked 虚线禁用态）；responsive.css ≤1024 卷体双栏收单列、≤780 卷头 source 满宽 + tag 横滑。
- 校验：node --check app.js OK；6 CSS 模块括号配对（sections 305/305、responsive 122/122）；关键标记齐全（#myth 锚点两处 + section + 3 卷轴 + 精卫 data-open-beast）。

## P4.5 神话叙事「强化章节感 + 沉浸阅读层」（完成）
- 决策：winner 在「单页 vs 多页」选择「保留单页 + 强化章节感」。方案 = 保留单页长卷主轴；给 #myth 加强章节感，三篇长文移出主页、改为可点开的沉浸阅读层（仿异兽抽屉），主页只留卷轴入口。
- HTML（index.html #myth）：新增 .myth-chapter 卷柒印章分隔带（双线 + 柒 金框）；三篇 <article> 改为 <button data-myth> 入口（卷头 + 导语 lede + 展卷阅读），移除内联卷体/卷脚长文；新增 <dialog id="myth-dialog"> 静态结构（close / head / original / prose / tags / link+locked）。
- CSS：.myth 背景加强为独立章节带（红径向 + 上下金线边）；新增 .myth-chapter 印章 / .myth-scroll 改 button 重置 / .myth-scroll__lede / .myth-scroll__open；新增 .myth-dialog 全套（居中阅读面板 + backdrop blur + 入场动画 myth-dialog-enter + 顶栏 accent 条由 JS 按故事设 --myth-accent-strong）。
- JS（app.js 末尾）：initMythLinks 改为先关 myth-dialog 再 openRecord；新增 initMythDialog IIFE——myths 数据对象（三故事原文/释义/意象/accent/beast），openMyth 填充 showModal，关闭（按钮/backdrop/ESC 原生）；夸父/刑天隐藏 link、显 locked。
- 校验：node --check app.js OK；6 CSS 模块括号配对（sections 344/344、responsive 126/126）；dialog 标签闭合（326 </dialog>）。
- 推送到 master（Pages 自动重建）。
- 未动 Hero/地图/legacy 结构与视觉；沿用设计语言。

## 下一阶段建议
- 仍可补录：海内·海外、大荒·海内异兽，解锁剩余 canon 锁定卡。
- 夸父/刑天目前是「待收录」叙事占位，若要变实，需先在 data.js 加 kuafu/xingtian 数据 + 原画，再让 myth-link 升级为 data-open-beast。

## 滚动揭示性能调优（完成）
- 现象：winner 反馈滑到板块时"等待约1s才出现"。排查确认非 Ajax/axios（项目纯静态、无网络请求），是 scroll-reveal 动画（IntersectionObserver + CSS transition）。
- 原参数：threshold 0.16 + rootMargin '0px 0px -8% 0px'（需露出16%且越过底8%收缩线才触发）+ transition .8s + translateY(26px)。
- 调优：app.js 触发改为 threshold 0.05 / rootMargin '0px 0px 0px 0px'（一露头即触发）；sections.css transition 0.8s→0.5s、translateY 26px→18px。整体更跟手、等待感降到约0.5s。
- 校验：node --check OK；git push master（d086f35）。如需完全零延迟，可进一步把 transition 降到 .3s 或全局关 reveal（仿 responsive.css 窄屏的 [data-reveal]{opacity:1!important;transform:none!important}）。

## 还原滚动揭示参数（完成）
- winner 澄清：之前只是疑惑网站是否用 Ajax，对原 scroll-reveal 节奏（露出16% + 动画0.8s）其实满意，要求改回。
- 还原：app.js 触发改回 threshold .16 / rootMargin '0px 0px -8% 0px'；sections.css 改回 transition .8s / translateY(26px)。已 node --check + git push master（1cad3d0）。
- 用户偏好（长期）：保留原有 scroll-reveal 节奏，不要为"更快/更跟手"自行调参，除非 winner 明确要求改。排疑类提问优先解释原因、不改默认。

## Hero 标本动态化（方案A，完成）
- winner 确认：让 Hero 右侧赤羽应龙原画（assets/dragon.png，静态 <img> in figure.specimen-art）变动态，采用方案A（CSS 氛围动效，不替换资源）。
- 实现：①龙图悬浮呼吸——用独立 translate/scale 属性做 img-float 9s（刻意不碰 transform，以免覆盖现有鼠标视差 transform:translate(--) 与换兽 is-leaving 的 transform）；②旋转金光衬底 .specimen-aura（conic-gradient + blur(24px) + mix-blend:screen + aura-rotate 16s）衬于框后；③火星粒子 .ember-field（app.js initEmbers 按屏宽注入 8/20 个 span，ember-rise 上浮淡出）。
- 无障碍：@media (prefers-reduced-motion: reduce) 关闭浮动/金光/粒子。
- 改动：index.html figure 内加 <span.specimen-aura> + <div.ember-field>；sections.css 加 img-float/aura-rotate/ember-rise keyframes + .specimen-aura/.ember-field/.ember + 减动效兜底；app.js 末尾 initEmbers IIFE。
- 校验：node --check OK；sections.css 括号 361/361 平衡；--accent-rgb 已定义（tokens.css:34，朱砂 185,38,32）。git push master（d729924）。
- 观测：线上 GitHub Pages 已同步；刷新即可见。若 winner 想更明显/更克制，可调 img-float 幅度(-12px→)、aura opacity(.55)、ember 数量/时长。

## Hero 标本切换为 video 槽位（方案B，完成）
- winner 选方案B（AE 渲染 WebM 循环替换图片）。本环境无 ffmpeg，无法渲染/生成 WebM；真实龙动画需 winner 在 AE（或 Remotion）导出 assets/dragon.webm 后放入即生效。
- 搭好前端视频槽位：index.html 的 <img id=specimen-image> 改为 <video autoplay muted loop playsinline poster="assets/dragon.png">，含 <source src="assets/dragon.webm" type="video/webm">；data.js yinglong 加 webm:'assets/dragon.webm' 字段；app.js changeSpecimenImage 重写为适配 video（设 poster；有 webm 则 image.src=webm+play().catch，无 webm 则 removeAttribute('src')+load() 退回静态 PNG；修掉 video 无 decode() 会 TypeError 的问题）；sections.css 拆分 .specimen-art img/video 基础样式（video 不加 img-float），加 .specimen-art video.is-leaving 淡出。
- 保留方案A 的 aura 金光旋转 + ember 火星叠加（video 之上继续增强）。library.js 的 image 是局部缩略图，与 hero 标本无关，无需改。
- git 事故与处理：首次 `git add -A` 误将仓库根无关目录 pv-remotion/（Remotion/React 视频工程，含 node_modules 共 13726 文件）一并提交，致 push 失败且弄脏本地 commit（ac4d232）。已 `git reset --soft HEAD~1 && git reset` 拆掉，新增 .gitignore 忽略 pv-remotion/ 与 node_modules/，重新只提交山海站文件（commit 2d720c8，push OK）。
- 校验：node --check app.js/data.js OK；sections.css 括号 363/363 平衡；git push master OK。
- 待 winner：用 AE/Remotion 导出 dragon.webm（建议 1240×1720@2x、4–8s 无缝循环、带墨底背景 #100a09、文件<4MB、静音），放入 assets/ 即自动播放；其他异兽将来各自加 webm 字段即可。当前无 webm 时 video 显示 poster 静态应龙图 + aura/ember 叠加（与方案A视觉相近，龙本身静态）。

## 方案B 龙 WebM 用 Remotion 程序化渲染（完成）
- winner 确认走 Remotion 路线。pv-remotion/ 是完整 Remotion 4 工程（node_modules 含 compositor-win32-x64-msvc，可本地 headless 渲染，无需 AE/ffmpeg）。
- 新增 pv-remotion/src/DragonLoop.tsx：以 dragon.png 为母体（1136×1385 RGB 无透明、自带墨底），程序化循环动画——龙身呼吸缩放+浮动+微旋、龙鳞金色流光扫描(soft-light)、旋转金光衬底(screen)、金尘/龙息粒子上浮、边缘暗角；周期 150 帧@30fps=5s 无缝（sin/取模保证端点视觉连续）。
- src/index.tsx 注册 id=DragonLoop Composition（1136×1385, fps30, dur150），保留原 MountainAndSeaPV。
- 渲染：`./node_modules/.bin/remotion render src/index.tsx DragonLoop out/dragon.webm --codec vp9 --crf 30`（remotion.config.ts 原设 h264，CLI --codec vp9 覆盖；62s，150/150 帧）；产物 out/dragon.webm 2.8MB。
- 接入：复制到 E:/Mountain and Sea/assets/dragon.webm；方案B video 槽位（index.html）立即生效，刷新即自动循环播放。校验：node 读 EBML 头 1a45dfa3 OK。git push master（7f032f0），GitHub Pages 同步。
- 注：dragon.png 无透明，故 WebM 带墨底整体动画，与原图构图/比例完全一致；方案A 的 aura 金光 + ember 火星仍叠加在 video 之上。其他异兽若要动，同法加 Composition + webm 字段即可。
