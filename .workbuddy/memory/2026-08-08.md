# 山海档案局 — 工作日志 2026-08-08

## P3 山经水经体系 + 文化影响（完成）
- 在「概述」与「古卷索引」间新增 #canon 板块：原创五方+四海罗盘 SVG（aria-hidden 装饰）+ 经部卡片网格（五藏山经5 + 海经3）。已收录经部（南山/北山/东山/东海）卡片带 data-go-filter，点击联动 P1 的 filter-chip 跳到 #records 并筛选；未收录（西/中山经、海内海外、大荒海内）加 is-locked 虚线禁用态，避免空态误导。
- 在「观测地图」后新增 #legacy 文化影响板块：三维卡（文学源流/影视游戏/学术研究）+ 历代校注时间线（郭璞/毕沅/郝懿行/袁珂）。
- 主导航与移动导航加「山经体系」「文化影响」锚点（桌面 nav gap 在 ≤1024 收到 18px 防拥挤）。
- JS（app.js 末尾 IIFE initCanonFilter）：监听 [data-go-filter] 点击，激活对应 chip、清空搜索、applyRecordsFilter，配合 href=#records 平滑滚动。
- CSS：sections.css 新增 .canon/.legacy 全套（复用墨底金线切角 clip-path、overview 标题风）；responsive.css 在 ≤1024/≤780/≤430 加罗盘/卡片/三卡/时间线堆叠适配。
- 校验：node --check app.js OK；6 模块 CSS 括号配对（sections 271/271、responsive 113/113）；SVG 标签平衡；data-go-filter 值与 filter-chip data-filter 完全一致。
- 未改 Hero/地图结构与视觉；沿用设计语言。
- 待 winner 本地自测（Live Server / python -m http.server 8211）。

## GitHub 部署上线（完成）
- 问题：git push 报 `Repository not found` / 连接超时，因 remote URL 连字符被复制为非 ASCII 字符（`The‑Classic‑of‑Mountains‑and‑Seas`）。
- 修复：`git remote set-url origin https://github.com/lzfxisxji/The-Classic-of-Mountains-and-Seas.git`（普通 ASCII 连字符）。
- 推送：`git push -u origin master` 成功，本地 master 跟踪 origin/master。
- 启用 GitHub Pages：`gh api -X POST repos/.../pages -f source[branch]=master -f source[path]=/`，仓库为 public，部署源 master 根目录。
- 访问地址：https://lzfxisxji.github.io/The-Classic-of-Mountains-and-Seas/
- 校验：仓库根目录包含 index.html、js/、css/、assets/，结构正确；Pages 首次构建状态 building，通常 1-3 分钟后可访问。
- 后续更新：本地修改后执行 `git push` 即可自动重新部署。

## 补内容：文案润色 + 解锁山经卡片（完成）
- 重写白泽/精卫/夔牛 research/quote：更扎实、有辨识度、去模板套话（data.js）。应龙保持不变。
- 新增 2 异兽：bifang 毕方（西山经·西次三经·火兆一足鸟，accent #c8531f）、fuzhu 夫诸（中山经·中次三经·四角白鹿大水兆，accent #3f8a78），字段齐全含 stats。
- 原创 SVG 档案原画：assets/bifang.svg、assets/fuzhu.svg（墨底+鎏金切角框+线描异兽+标题印章，对齐现有 PNG 原画风，作抽屉大图 img 源）。
- HTML 接入：record-rail 加 2 张 .record-card（含 card-art 线描 SVG + data-category 西山经/中山经）；map 加 2 个 .map-region 节点 + 2 条 map-route；filter-chip 加 西山经/中山经；records 计数 04→06、map 标题 04→06 SEALED MARKERS。
- canon 解锁：西山经/中山经 从 is-locked 改可点击 a canon-card + data-go-filter="西山经/中山经"，计数改「已收录 1 异兽」。海内·海外、大荒·海内仍锁定。
- CSS：components.css 加 .bifang-art（火橙）/ .fuzhu-art（青绿）描边色。
- 校验：data.js ES module 语法 OK；6 CSS 模块括号配对（components 81/81 等）；2 SVG 标签平衡；record-card↔filter-chip↔canon go-filter 完全一致（6 卡 / 7 chip / 6 联动）。
- 未改 hero（仍应龙）。相关档案按钮随 beasts 自动包含新兽。

## P4 神话叙事板块（完成）
- 位置：观测地图(#map) 与 文化影响(#legacy) 之间新增 #myth 板块，形成「地理→故事→影响」叙事递进。
- 主导航 + 移动导航在「观测地图」后加「神话叙事」(#myth) 锚点（桌面 nav gap ≤1024 已收到 18px，容纳无碍）。
- 三卷轴结构（复用 data-reveal 滚动揭示 + 切角 clip-path + 墨底金线）：卷头（MYTH·NO + 故事名 + 经部出处）/ 卷体（古籍原文左 朱砂边 / 现代释义右）/ 卷脚（意象 tag + 关联按钮）。
- 三故事：精卫填海（--jingwei 朱砂，data-open-beast="jingwei" 关联已存在的 jingwei 档案抽屉）/ 夸父逐日（--kuafu 橙金，待收录）/ 刑天舞干戚（--xingtian 铁青，待收录）。夸父、刑天无档案数据，仅做「异兽待收录」虚线禁用标注。
- JS（app.js 末尾 IIFE initMythLinks）：监听 [data-open-beast] 点击 → openRecord(beast) 复用现有抽屉逻辑（无写新兽数据）。
- CSS：sections.css 新增 .myth / .myth-scroll（--myth-accent/--myth-accent-strong 三色变量）/ 卷头卷体卷脚 / .myth-tag / .myth-link（含 --locked 虚线禁用态）；responsive.css ≤1024 卷体双栏收单列、≤780 卷头 source 满宽 + tag 横滑。
- 校验：node --check app.js OK；6 CSS 模块括号配对（sections 305/305、responsive 122/122）；关键标记齐全（#myth 锚点两处 + section + 3 卷轴 + 精卫 data-open-beast）。

## P4.5 神话叙事「强化章节感 + 沉浸阅读层」（完成）
- 决策：winner 在「单页 vs 多页」选择「保留单页 + 强化章节感」。方案 = 保留单页长卷主轴；给 #myth 加强章节感，三篇长文移出主页、改为可点开的沉浸阅读层（仿异兽抽屉），主页只留卷轴入口。
- HTML（index.html #myth）：新增 .myth-chapter 卷柒印章分隔带（双线 + 柒 金框）；三篇 <article> 改为 <button data-myth> 入口（卷头 + 导语 lede + 展卷阅读），移除内联卷体/卷脚长文；新增 <dialog id="myth-dialog"> 静态结构（close / head / original / prose / tags / link+locked）。
- CSS：.myth 背景加强为独立章节带（红径向 + 上下金线边）；新增 .myth-chapter 印章 / .myth-scroll 改 button 重置 / .myth-scroll__lede / .myth-scroll__open；新增 .myth-dialog 全套（居中阅读面板 + backdrop blur + 入场动画 myth-dialog-enter + 顶栏 accent 条由 JS 按故事设 --myth-accent-strong）。
- JS（app.js 末尾）：initMythLinks 改为先关 myth-dialog 再 openRecord；新增 initMythDialog IIFE——myths 数据对象（三故事原文/释义/意象/accent/beast），openMyth 填充 showModal，关闭（按钮/backdrop/ESC 原生）；夸父/刑天隐藏 link、显 locked。
- 校验：node --check app.js OK；6 CSS 模块括号配对（sections 344/344、responsive 126/126）；dialog 标签闭合（326 </dialog>）。
- 推送到 master（Pages 自动重建）。
- 未动 Hero/地图/legacy 结构与视觉；沿用设计语言。

## 下一阶段建议
- 仍可补录：海内·海外、大荒·海内异兽，解锁剩余 canon 锁定卡。
- 夸父/刑天目前是「待收录」叙事占位，若要变实，需先在 data.js 加 kuafu/xingtian 数据 + 原画，再让 myth-link 升级为 data-open-beast。
