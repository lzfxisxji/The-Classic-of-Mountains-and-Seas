# 2026-08-10

## 修改记录
- **Hero 换兽同步 bug 修复**：`<video>` 在播放过 `dragon.webm` 后移除 `src` 不会自动刷新 poster，导致左侧切到无 webm 的异兽（如夔牛）时右侧仍显示应龙。新增 `#specimen-fallback` `<img>` 作为兜底：有 webm 时隐藏 fallback 播放 video，无 webm 时显示 fallback 静态图。
- **隐藏档案索引卡片简笔画**：`.card-art` 加 `display: none`，保留 HTML 结构以备后续恢复。
- **交换「档案索引」与「上古地理博物志」位置**：页面顺序改为 Hero → #records → #overview → #canon → #map → #myth → #legacy；同步调整顶部主导航、移动端导航与 Hero 向下滚动入口（`hero-scroll` 指向 `#records`）。
- **校验与推送**：`node --check` OK；CSS 括号平衡；commit `35cc409`，GitHub Pages 已同步。

## Bug 修复（续）
- **白泽等无 webm 异兽点击后右侧空白**：根因是 `.specimen-fallback` 同时匹配 `.specimen-art img`（设 `position: relative; z-index:1`，特异性 0,1,1）与自身规则（`position: absolute`，特异性 0,1,0），前者优先级高使 fallback 实际为 relative、`inset:0` 失效、脱离绝对叠放，video 隐藏后 fallback 定位错乱被裁出框外，只剩墨底空框。修复：把 fallback 选择器提升为 `.specimen-art .specimen-fallback`（0,2,0，覆盖 `.specimen-art img`）+ `.is-visible` 一并提升；fallback `z-index` 提到 2，确保盖在 video 之上。CSS 括号 370/370 平衡；commit `53b7e2c` 已推送。

## Hero 改回静态图 + 分层交互应龙
- **需求1 改回图片**：应 winner 要求，Hero 从 `<video>` 改回 `<img>`（注释掉 index.html 的 video 槽位、data.js 的 `yinglong.webm`、app.js 的 video 专属逻辑，均保留注释备用）。换兽逻辑还原为纯 `img.src` 赋值，`changeSpecimenImage` 去掉 pause/play/load。commit `9bd0c8b`。
- **需求2 分层交互应龙（鼠标热区驱动局部动画）**：winner 选「分层 SVG 重绘」方案。在 `.specimen-art` 内联 `<svg id="specimen-svg">`（墨线鎏金风格），尾/身/左翼/右翼/头/前后爪各自独立 `<g class="part" data-part>` 含可见 `.art` 路径 + 透明 `.hot` 热区矩形。CSS：`.specimen-art.is-interactive` 时显示 svg、隐藏静态 img；各部位 `transform-box:fill-box` + 各自 `transform-origin`；常态 body-breathe/tail-sway 无限循环，hover 触发 wing-flap-left/right、head-nod、body-breathe、tail-sway、claw-grip 专属动画 + 鎏金 drop-shadow 高亮；`prefers-reduced-motion` 关闭动画。JS：`data.js` 给 yinglong 加 `interactive:true`；`app.js` 加 `setInteractive(beast)`（应龙加 `is-interactive`、其他异兽移除并清除 active）、`initSpecimenInteraction()`（mouseenter/leave 加/去 `is-active`，触屏 click 切换），并在 `changeSpecimenImage` 与初始化时调用。commit `afeb3e9`（已推送 GitHub Pages）。
- 注意：Bash 工具在本会话偶发「command received undefined」序列化错误，改用 PowerShell 执行命令；PowerShell 的 stdout 不回显，需用 `Out-File -Encoding ascii` 落盘后用 Read 查看。git 推送用 `gh auth setup-git` 提供凭据。

## 取消异兽呼吸 + 修复 #myth/#legacy 消失
- **需求1 取消呼吸效果**：移除三处常态缩放——`.beast` 的 `beast-breathe`（整图呼吸缩放）、`.specimen-art img/.specimen-fallback` 的 `img-float`（漂浮缩放 1.02）、应龙 body 常温 `body-breathe`。保留 `tail-sway` 轻摆（非缩放）与 hover/active 交互缩放（翅膀挥动、头点、尾摆、爪抓、悬停身体呼吸）。新增静态档案图 `:not(.is-interactive) img:hover` 放大 1.04、hero 大图 `.beast:hover` 放大 1.03，呼应「图片只有鼠标移动时才变大变小」。删除孤立 `@keyframes img-float`（sections.css）、`beast-breathe`（animations.css）。
- **需求2 #myth/#legacy 消失的根因**：滚动揭示依赖 JS，目标区块子元素 `[data-reveal]` 默认 `opacity:0`；线上版本（afeb3e9）的 app.js 缺 `revealHashTarget` 锚点显影兜底，IntersectionObserver 在点击导航跳转 / 平滑滚动到底部时，目标区块子元素未及时显影，整块看似消失。将 `initReveal` 改为多保险：IO（threshold 降到 0.12、rootMargin -6%）+ 初始化即时显影视口内元素 + 锚点跳转/hashchange 后立即显影目标 + 滚动结束（140ms 节流）兜底 + load 后再确认。校验：JS module 语法 OK，sections.css/animations.css 括号平衡。待提交推送。
